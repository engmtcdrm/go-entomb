services:
  devcontainer:
    image: mcr.microsoft.com/devcontainers/go:2-1.25-trixie
    env_file:
      - .env
    volumes:
      - ~/.ssh:/home/vscode/.ssh:ro
      - ..:/workspaces/go-entomb:cached
    command: sleep infinity
